# Блог «Лонгрид? Коротко!» — Финальный исходный код

> **Вариант 09** — Full-stack приложение для создания и публикации блог-постов с поддержкой черновиков, тегов, комментариев и лайков.

[![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue)](https://www.typescriptlang.org/)
[![React](https://img.shields.io/badge/React-18-61dafb)](https://reactjs.org/)
[![Express](https://img.shields.io/badge/Express-5-green)](https://expressjs.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-14-336791)](https://www.postgresql.org/)

---

## 📖 О проекте

Современное веб-приложение для создания и публикации длинных статей (лонгридов) с удобным редактором, системой тегов, комментариев и лайков.

### Ключевые возможности

- ✅ **Аутентификация и авторизация** — JWT токены, роли (user, admin)
- ✅ **Редактор постов** — Markdown поддержка, черновики
- ✅ **Система тегов** — Многие-ко-многим связь
- ✅ **Комментарии** — К опубликованным постам
- ✅ **Лайки** — Один пользователь = один лайк
- ✅ **Пагинация** — Удобная навигация по спискам
- ✅ **Безопасность** — CORS, Helmet, bcrypt
- ✅ **Кэширование** — Redis для оптимизации
- ✅ **Мониторинг** — Логи (Winston) + Метрики (Prometheus)

---

## 🚀 Инструкция по запуску

### Требования

- **Node.js**: 18.x или выше
- **Docker** и **Docker Compose**: последняя версия (для запуска PostgreSQL и Redis)
- **npm** или **yarn**

### Вариант 1: Быстрый запуск с Docker Compose (рекомендуется)

Этот способ автоматически запускает все компоненты приложения (frontend, backend, PostgreSQL, Redis):

```bash
# 1. Перейти в папку проекта
cd students/YarmolaAleksandr/task_02

# 2. Запустить все сервисы
docker-compose up --build -d

# 3. Дождаться готовности всех сервисов (примерно 30-60 секунд)
```

**Приложение доступно по адресам:**

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:5000
- **Swagger документация**: http://localhost:5000/api-docs

### Вариант 2: Ручной запуск (для разработки)

#### Шаг 1: Запуск базы данных

```bash
# Запустить только PostgreSQL и Redis
docker-compose up postgres redis -d
```

#### Шаг 2: Установка зависимостей

```bash
# Установить зависимости для всего проекта (из корня task_02/)
npm install
```

#### Шаг 3: Настройка базы данных (Backend)

```bash
# Перейти в папку сервера
cd src/apps/server

# Сгенерировать Prisma Client
npm run prisma:generate

# Применить схему к базе данных
npm run prisma:push

# (Опционально) Наполнить базу тестовыми данными
npm run seed
```

#### Шаг 4: Запуск Backend

```bash
# Из папки src/apps/server
npm run dev
```

Backend будет доступен на http://localhost:5000

#### Шаг 5: Запуск Frontend

```bash
# Перейти в папку frontend (из корня проекта)
cd src/apps/web

# Запустить dev-сервер
npm start
```

Frontend будет доступен на http://localhost:3000

---

## 🏗️ Структура проекта

```
task_02/
├── src/                    # Исходный код приложения
│   ├── apps/              # Приложения
│   │   ├── server/        # Backend (Express + Prisma)
│   │   └── web/           # Frontend (React + TypeScript)
│   ├── docs/              # Документация
│   ├── e2e/               # E2E тесты (Playwright)
│   └── k8s/               # Конфигурация Kubernetes
├── docker-compose.yaml    # Конфигурация Docker Compose
├── package.json           # Зависимости проекта
├── playwright.config.ts   # Конфигурация тестов
└── README.md             # Этот файл
```

---

## 🧪 Запуск тестов

### E2E тесты (Playwright)

```bash
# Убедитесь, что приложение запущено (docker-compose up)
# Затем запустите тесты:
npm run test:e2e
```

Или используйте PowerShell скрипт:

```powershell
.\test-e2e.ps1
```

---

## 🛠️ Технологический стек

| Компонент | Технологии |
|-----------|------------|
| **Frontend** | React 18, TypeScript, React Router v6, Axios |
| **Backend** | Node.js 18, Express 5, TypeScript, Prisma ORM |
| **Database** | PostgreSQL 14, Redis 7 |
| **Auth** | JWT, bcrypt |
| **Security** | Helmet, CORS |
| **Logging** | Winston |
| **Metrics** | Prometheus (prom-client) |
| **Testing** | Jest, Playwright |
| **Container** | Docker, Docker Compose |
| **Orchestration** | Kubernetes |

---

## 📚 Дополнительная документация

- [Архитектура проекта](src/docs/ARCHITECTURE.md)
- [API документация](http://localhost:5000/api-docs) (после запуска)

---

## 🎯 Основные функции

1. **Регистрация и аутентификация** — создание аккаунта, вход, JWT-токены
2. **Создание постов** — редактирование в Markdown, черновики, публикация
3. **Теги** — добавление нескольких тегов к посту
4. **Комментарии** — к опубликованным постам
5. **Лайки** — один пользователь может поставить один лайк посту
6. **Административная панель** — управление пользователями и контентом (для роли admin)

---

## ⚙️ Переменные окружения

При необходимости можно настроить переменные окружения в `src/apps/server/.env`

```env
DATABASE_URL="postgresql://user:password@localhost:5432/blog_db"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="your-secret-key"
PORT=5000
```

---

## 📝 Примечания

- Проект использует **Node.js 18** — убедитесь, что у вас установлена правильная версия
- При первом запуске сборка Docker может занять несколько минут
- Для production-деплоя смотрите [документацию Kubernetes](src/k8s/)
